import React from "react";

const SearchBox = () => {
  return (
      <input
        className="form-control"
        type="search"
        placeholder="search products"
      />
  );
};

export default SearchBox;
