import React from "react";

const SearchBox = () => {
  return (
      <input
        className="form-control"
        type="search"
        placeholder="search product"
      />
  );
};

export default SearchBox;
